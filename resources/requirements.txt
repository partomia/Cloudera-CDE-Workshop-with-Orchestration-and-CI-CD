# Intentionally minimal — CDE Spark runtime already bundles pyspark, pandas, pyarrow, boto3.
# great-expectations causes build timeouts in CDE Python env; validation uses native Spark fallback.
# Add packages here only when a job requires something not in the base CDE runtime.
